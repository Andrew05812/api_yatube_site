# Быстрый запуск Kittygram

## 🚀 Самый простой способ - двойной клик!

### Для запуска всего приложения:
**Дважды кликните на файл:** `start_all.bat`

Это запустит оба сервера (Backend и Frontend) в отдельных окнах.

### Для запуска только Backend:
**Дважды кликните на файл:** `start_backend.bat`

### Для запуска только Frontend:
**Дважды кликните на файл:** `start_frontend.bat`

### Для создания тестовых данных:
**Дважды кликните на файл:** `create_sample_data.bat`

---

## 📋 Что происходит при запуске:

### `start_all.bat`
- Запускает Django сервер на http://127.0.0.1:8000
- Запускает React сервер на http://localhost:3000
- Открывает два отдельных окна командной строки

### `start_backend.bat`
- Активирует виртуальное окружение Python
- Проверяет и выполняет миграции базы данных
- Запускает Django сервер

### `start_frontend.bat`
- Проверяет наличие node_modules (устанавливает если нужно)
- Запускает React dev server

### `create_sample_data.bat`
- Создает тестового пользователя: `demo_user` / `demo123`
- Создает 15 котиков с фотографиями и достижениями

---

## 🌐 После запуска:

1. Откройте браузер
2. Перейдите на **http://localhost:3000**
3. Зарегистрируйтесь или войдите как `demo_user` / `demo123`

---

## ⚠️ Важно:

- При первом запуске может потребоваться установка зависимостей
- Убедитесь, что порты 8000 и 3000 свободны
- Для остановки серверов закройте окна или нажмите Ctrl+C

---

## 🔧 Первоначальная настройка (только один раз):

Если вы запускаете приложение впервые:

1. **Установите зависимости Backend:**
   ```bash
   env\Scripts\activate
   pip install -r requirements.txt
   ```

2. **Установите зависимости Frontend:**
   ```bash
   cd frontend
   npm install
   ```

3. **Выполните миграции:**
   ```bash
   python manage.py migrate
   ```

4. **Создайте тестовые данные:**
   Дважды кликните на `create_sample_data.bat`

После этого можно использовать скрипты запуска!


# Kittygram - Полнофункциональное приложение

Это объединенный проект, содержащий Django backend и React frontend.

## 🚀 Быстрый запуск (Windows)

**Самый простой способ:** Дважды кликните на файл `start_all.bat` - это запустит оба сервера автоматически!

Подробнее см. файл [README_START.md](README_START.md)

## Структура проекта

```
kittygram_backend-main/
├── cats/                    # Django приложение для работы с котиками
├── kittygram_backend/       # Настройки Django проекта
├── frontend/                # React приложение
│   ├── src/                # Исходный код React
│   ├── public/             # Публичные файлы
│   └── package.json        # Зависимости Node.js
├── manage.py               # Django management script
└── requirements.txt        # Python зависимости
```

## Установка и запуск

### Предварительные требования

- Python 3.8+
- Node.js 14+ и npm
- pip (менеджер пакетов Python)

### Шаг 1: Настройка Backend (Django)

1. Перейдите в директорию проекта:
```bash
cd kittygram_backend-main
```

2. Создайте и активируйте виртуальное окружение:

**Windows:**
```bash
python -m venv env
env\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv env
source env/bin/activate
```

3. Установите зависимости:
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

4. Выполните миграции:
```bash
python manage.py migrate
```

5. (Опционально) Создайте суперпользователя:
```bash
python manage.py createsuperuser
```

### Шаг 2: Настройка Frontend (React)

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

### Шаг 3: Запуск приложения

#### Режим разработки (Development)

Запустите два сервера в разных терминалах:

**Терминал 1 - Django Backend:**
```bash
cd kittygram_backend-main
python manage.py runserver
```
Backend будет доступен по адресу: http://127.0.0.1:8000

**Терминал 2 - React Frontend:**
```bash
cd kittygram_backend-main/frontend
npm start
```
Frontend будет доступен по адресу: http://localhost:3000

#### Режим production

1. Соберите React приложение:
```bash
cd kittygram_backend-main/frontend
npm run build
```

2. Запустите Django сервер:
```bash
cd kittygram_backend-main
python manage.py collectstatic --noinput
python manage.py runserver
```

Приложение будет доступно по адресу: http://127.0.0.1:8000

## Переменные окружения

Создайте файл `.env` в корне проекта `kittygram_backend-main/` для настройки:

```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOW_ALL_ORIGINS=True
```

Для frontend создайте файл `.env` в `frontend/`:

```env
REACT_APP_API_URL=http://127.0.0.1:8000
```

## API Endpoints

- `GET /api/cats/` - Список котиков (с пагинацией)
- `GET /api/cats/{id}/` - Детали котика
- `POST /api/cats/` - Создать нового котика
- `PATCH /api/cats/{id}/` - Обновить котика
- `DELETE /api/cats/{id}/` - Удалить котика
- `POST /api/users/` - Регистрация пользователя
- `POST /api/token/login/` - Вход (получить токен)
- `POST /api/token/logout/` - Выход
- `GET /api/users/me/` - Информация о текущем пользователе

## Разработка

### Запуск тестов

**Backend:**
```bash
python manage.py test
```

**Frontend:**
```bash
cd frontend
npm test
```

### Создание миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

## Деплой

Для деплоя на production сервер:

1. Установите `DEBUG=False` в настройках
2. Настройте `ALLOWED_HOSTS` с вашим доменом
3. Соберите React приложение: `npm run build`
4. Соберите статические файлы Django: `python manage.py collectstatic`
5. Используйте production WSGI сервер (например, gunicorn)

## Полезные команды

- `python manage.py runserver` - Запуск Django dev сервера
- `npm start` - Запуск React dev сервера
- `npm run build` - Сборка React приложения для production
- `python manage.py createsuperuser` - Создание администратора
- `python manage.py shell` - Django shell
- `python manage.py create_sample_data` - Создание тестовых данных (котиков с фотографиями)

## Структура базы данных

Проект использует SQLite по умолчанию (для разработки). Для production рекомендуется использовать PostgreSQL.

## Поддержка

При возникновении проблем проверьте:
1. Установлены ли все зависимости
2. Запущены ли миграции
3. Правильно ли настроены переменные окружения
4. Запущены ли оба сервера (в режиме разработки)
